language: java
install:
- mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

script:
 - mvn test

after_success:
 - docker build -t julienbtn/java-image .
 - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD";
 - docker push "$DOCKER_USERNAME"/cloud-java;
